{
    "name": "Java",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "vscjava.vscode-java-pack"
            ]
        }
    },
    "postCreateCommand": "java -version && mvn -version && make --version",
    "remoteUser": "vscode"
}